import React from 'react';
import {StyleSheet, Text} from 'react-native';

class Defi extends React.Component {
    render(){
        return (
            <Text> OUI BONJOUR </Text>
        )
    }
}

const styles = StyleSheet.create({

})

export default Defi;